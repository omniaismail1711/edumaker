
## Replace Header Background Image

Replace the current hero background image (`src/assets/hero-bg.png`) with the new uploaded image across the site.

### Steps

1. Copy the new image `user-uploads://Gemini_Generated_Image_1f21di1f21di1f21.png` to `src/assets/hero-bg.png` (overwriting the existing one)
2. No code changes needed since `Index.tsx` already imports from `@/assets/hero-bg.png`

This is a simple asset swap -- the new image will automatically appear in the hero section on the landing page.
